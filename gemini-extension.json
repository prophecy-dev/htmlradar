{
  "name": "htmlradar",
  "version": "0.4.0",
  "description": "Publish the HTML Gemini just wrote as a tracked link, then ask who opened it and which sections they read.",
  "mcpServers": {
    "htmlradar": {
      "command": "npx",
      "args": ["-y", "htmlradar-mcp"],
      "env": {
        "HTMLRADAR_API_KEY": "$HTMLRADAR_API_KEY"
      }
    }
  },
  "settings": [
    {
      "name": "HTMLRadar API key",
      "description": "Created at https://htmlradar.com/settings under \"API keys\". Starts with hr_live_.",
      "envVar": "HTMLRADAR_API_KEY",
      "sensitive": true
    }
  ]
}
