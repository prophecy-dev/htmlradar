{
  "name": "@htmlradar/db",
  "version": "0.1.0",
  "private": true,
  "description": "D1 schema and data access shared by the proxy worker and the dashboard.",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./owner": "./src/owner.ts",
    "./public": "./src/public.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "migrate:local": "wrangler d1 migrations apply htmlradar --local"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240314.0",
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  }
}
