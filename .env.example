# Somnia-internal fork: there is no single root env any more.
#   packages/app/.env.example        dashboard (Cloudflare Access, SESSION_SECRET, NEXT_PUBLIC_*)
#   packages/proxy/wrangler.jsonc    recipient worker vars; secrets via `wrangler secret put`
# Deploy credentials live in the gitignored repo-root .env (see INTERNAL.md).
